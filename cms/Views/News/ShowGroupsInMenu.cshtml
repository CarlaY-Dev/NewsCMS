@model IEnumerable<DataLayer.PageGroup>

<span>گروه های خبری</span>
<ul>
    @foreach (var item in Model)
    {
        <li><a href="/Groups/@item.GroupID/@item.GroupTitle">@item.GroupTitle</a></li>
    }

</ul>